# 바다송금 v2.0 구현 작업서

> 이 문서는 바다송금 v1.0을 보존하면서 v2.0 개발을 시작하기 위한 작업 기준임.
> 새 작업 세션에서 이 문서를 먼저 읽고 현재 저장소를 분석한 뒤 구현을 진행할 것.

## 1. v2.0 서비스 방향

기존의 `고령 사용자를 위한 송금 연습 서비스`를 다음 방향으로 확장함.

> 연령이나 장애 유형으로 사용자를 구분하지 않고, 개인별 디지털 금융 숙련도와 필요한 지원 방식에 따라 안내 수준을 조정하며, 모의 송금을 반복해 일반 금융 서비스의 용어와 화면 구조에 점진적으로 적응하도록 돕는 서비스

대상을 특정 집단으로 고정하지 않음.

- 모바일 금융 경험이 부족한 사용자
- 금융 용어가 익숙하지 않은 사용자
- 고령 사용자
- 시각·청각·운동·인지 지원이 필요한 사용자
- 한국어 금융 용어와 국내 송금 절차가 익숙하지 않은 사용자

한 사용자가 고령이면서 저시력일 수 있는 것처럼 여러 특성이 중첩될 수 있음. 따라서 `isElderly`, `isDisabled`, `isForeigner`처럼 사람을 분류하는 모델을 만들지 않고, UI에 필요한 지원 기능을 조합하는 방식으로 설계함.

## 2. v1.0 보존 원칙

v1.0은 프론트엔드 mock MVP 구현 완료 상태임. v2.0 개발 중 다음 동작을 훼손하지 않음.

- 홈 안내
- 연습 송금 및 가상 송금 흐름
- 수취인 선택과 계좌 직접 입력
- 금액 입력과 한글 금액 표시
- 조건부 안전 확인
- 최종 수취인·금액 재확인
- 완료 영수증과 오송금 대응 안내
- 최근 내역과 즐겨찾기
- 큰 글씨와 음성 안내
- 가족·도우미 초기 설정
- 기존 단위 테스트와 CI

v2.0은 별도 앱을 새로 만드는 방식이 아니라 기존 송금 도메인을 공유하면서 안내 표현 정책을 확장하는 방식으로 구현함.

## 3. 핵심 가설

아래 내용은 검증된 성과가 아닌 v2.0 검증 가설임.

1. 사용자가 필요한 안내 수준을 직접 선택할 수 있으면 서비스 진입 부담을 줄일 수 있음
2. 쉬운 표현과 일반 금융 용어를 단계적으로 병기하면 일반 금융 용어 학습을 지원할 수 있음
3. 반복 연습 결과를 근거로 안내 축소를 제안하면 표준 금융 화면으로의 점진적 전환을 지원할 수 있음

실사용자 테스트 전까지 효과를 확정적으로 표현하지 않음.

## 4. 오늘 진행할 작업

다음 다섯 작업을 순서대로 진행함.

### 4.1 `docs/V2_PRODUCT_BRIEF.md` 작성

다음 내용을 포함함.

- v1.0과 v2.0의 차이
- v2.0 서비스 정의
- 핵심 사용자와 교차 특성
- 사용자를 연령·장애·국적으로 직접 분류하지 않는 이유
- 핵심 가설 3개
- 이번 구현 범위와 제외 범위
- 개인정보 최소 수집 원칙
- 안내 수준과 관계없이 유지할 금융 안전장치
- 사용자 검증 계획
- 판단할 수 없는 내용의 `[확인 필요]` 표시

문체는 `~함`, `~필요`, `~제외`, `~예정` 형태로 작성함.

### 4.2 핵심 가설을 측정 가능한 형태로 구체화

가설마다 다음 항목을 정의함.

- 관찰할 사용자 행동
- 성공 판단 기준
- 필요한 이벤트
- 현재 측정 가능한 범위
- 실제 사용자 테스트가 필요한 범위

응답시간만으로 사용자의 숙련도를 판단하지 않음.

### 4.3 `AssistanceProfile` 모델 구현

아래 개념을 출발점으로 현재 구조에 맞게 구현함.

```ts
type GuidanceLevel = "detailed" | "essential" | "standard";
type SupportedLanguage = "ko" | "en";

interface AssistanceProfile {
  guidanceLevel: GuidanceLevel;
  preferredLanguage: SupportedLanguage;
  textScale: "default" | "large";
  isVoiceGuideEnabled: boolean;
  isHighContrastEnabled: boolean;
  isReducedMotionEnabled: boolean;
}
```

요구사항:

- 기존 접근성 설정과 중복되는 상태를 확인하고 통합
- 동일한 상태를 두 곳에서 관리하지 않도록 구성
- 기존 localStorage 데이터와 호환
- 손상되거나 이전 형식인 저장값은 안전한 기본값으로 복구
- 장애 여부, 연령, 국적은 저장하지 않음
- 타입, 저장 로직, UI 상태 업데이트 책임 분리
- 아직 필요하지 않은 전역 상태 라이브러리는 추가하지 않음

### 4.4 v1.0 E2E 기준선 구축

현재 테스트 도구와 스크립트를 먼저 확인한 뒤 기존 도구를 우선 재사용함.

최소 시나리오:

- 홈에서 연습 송금 시작
- 수취인 선택
- 금액 입력
- 조건부 안전 확인
- 최종 확인
- 송금 완료
- 수취인 변경 시 금액·위험·확인 상태 초기화
- 49만 9천 원과 50만 원의 위험 기준 경계
- 이전 단계 이동
- 완료 화면 새로고침
- 큰 글씨 모드
- 가능한 범위의 키보드 탐색

자동화하기 어려운 항목은 억지로 테스트하지 않고 이유와 수동 확인 방법을 기록함.

### 4.5 안내 방법 선택 화면 구현

사용자에게 연령, 장애, 국적을 묻지 않음.

안내 수준:

- 하나씩 자세히 설명
- 필요한 내용만 설명
- 일반 금융 앱처럼 사용

추가 지원:

- 글자를 크게 표시
- 화면 내용을 음성으로 안내
- 높은 명도 대비 사용
- 화면 움직임 최소화

요구사항:

- 초기 설정 이후 언제든 변경 가능
- 자세한 안내 수준으로 언제든 복귀 가능
- 선택하지 않아도 안전한 기본값 제공
- 키보드와 스크린 리더로 조작 가능
- 설정 저장 실패가 송금 기능 실패로 이어지지 않도록 처리
- 기존 디자인 시스템과 모바일 레이아웃 유지
- 설명량이 줄어도 수취인·금액·위험 거래 확인 유지

## 5. 점진적 적응 구조

세 안내 수준이 별도의 송금 기능을 가지면 안 됨.

공유할 도메인 로직:

- 수취인
- 계좌
- 금액
- 위험 신호
- 최종 확인
- 거래 결과

안내 수준에 따라 변경 가능한 표현:

- 설명 문장
- 일반 금융 용어 노출 정도
- 도움말과 예시
- 한 화면의 정보량

용어 전환 예시:

| 단계 | 표시 용어 | 보조 설명 |
|---|---|---|
| 자세히 안내 | 받는 분 | 은행에서는 돈을 받는 사람을 `수취인`이라고 부름 |
| 핵심만 안내 | 받는 분(수취인) | 돈을 받을 계좌 확인 |
| 표준 화면 | 수취인 | 수취인명과 계좌번호 확인 |

안내 수준은 자동 변경하지 않음. 반복 과업 결과를 근거로 축소를 제안하되 사용자가 직접 결정하며 언제든 이전 단계로 돌아갈 수 있어야 함.

## 6. 학습 이벤트와 응답시간

초기에는 의미 있는 행동만 기록함.

- 단계 시작과 완료
- 도움말 열기
- 수취인 재선택
- 금액 수정
- 이전 단계 이동
- 금융 용어 확인 결과
- 안내 수준 변경과 복귀

수집하지 않을 데이터:

- 장애 여부
- 연령과 국적
- 실제 계좌번호
- 수취인 실명
- 전체 키 입력값
- 음성 내용

응답시간은 다음 요소를 분리함.

```text
유효 상호작용 시간
= 전체 체류시간
- 네트워크 대기시간
- 음성 안내 재생시간
- 백그라운드 체류시간
```

느린 응답을 낮은 숙련도로 단정하지 않음. 완료 여부, 수정 횟수, 도움말 사용, 이전 단계 이동, 용어 확인 결과와 함께 사용하는 보조 신호로 제한함.

다음 안전장치는 숙련도와 관계없이 축소하지 않음.

- 최종 수취인 확인
- 최종 금액 확인
- 위험 상황 확인
- 거래 실행 동의
- 오류와 미확정 거래 안내

## 7. 이번 단계 제외 범위

- 실제 계좌와 금융기관 API 연동
- AI 기반 숙련도 판정
- 응답시간 기반 자동 등급 변경
- 사용자 동의 없는 안내 수준 변경
- 장애 정보 수집
- 자동 번역 API 기반 금융 확인 문구
- 실거래 인증·인가
- 실제 거래 원장
- 실제 금융기관 수준의 FDS

## 8. 선택적 백엔드 확장

v2.0 초기 작업은 기존 Next.js 프론트엔드에서 진행함. 다음 요구가 발생할 때만 백엔드를 추가함.

- 학습 기록을 여러 기기에서 유지할 필요
- 사용자 지원 프로필 영속화
- 모의 거래의 중복 요청 방지
- 거래 상태와 실패·지연 시나리오 구현
- 서버 감사 로그와 데이터 접근 통제 검증

백엔드 추가 시 예정 구성:

- Spring Boot
- Spring Security
- PostgreSQL
- OpenAPI
- Testcontainers
- Docker
- Railway 배포

Railway는 실제 금융 서비스를 운영하기 위한 인프라가 아니라 포트폴리오용 모의 API와 PostgreSQL의 배포 환경으로만 사용함. 실제 계좌정보와 실자금을 처리하지 않음.

예정 API:

```text
POST  /practice-sessions
POST  /practice-transfers
GET   /practice-transfers/{transactionId}
POST  /learning-events
GET   /users/me/assistance-profile
PATCH /users/me/assistance-profile
```

모의 거래 서버를 구현할 경우 다음 항목을 우선 검증함.

- 서버 입력값 검증
- 멱등성 키와 중복 요청 방지
- `REQUESTED / PROCESSING / SUCCEEDED / FAILED / UNKNOWN` 상태 모델
- 네트워크 타임아웃 후 거래 상태 재조회
- 감사 로그의 민감정보 제외
- 클라이언트 검증과 서버 검증의 역할 분리

Spring Boot, PostgreSQL, Railway는 실제 구현·배포 전까지 포트폴리오에서 `사용 기술`이 아니라 `향후 확장`으로 표시함.

## 9. 구현 원칙

1. 작업 전 `AGENTS.md`, `README.md`, `docs/PRODUCT_BRIEF.md`, `DEVELOPMENT_LOG.md`를 먼저 확인
2. v1.0 동작과 기존 사용자 변경사항 보존
3. 비즈니스 로직과 안내 표현 정책 분리
4. 미래 확장을 이유로 불필요한 디렉터리와 인터페이스를 미리 만들지 않음
5. boolean은 `is`, `has`, `should` 접두사 사용
6. 이벤트 핸들러는 `handle` 접두사 사용
7. 매직 값은 이름 있는 상수로 추출
8. 실제 구현과 계획을 명확히 구분
9. 통계·사용자 조사·검증 결과를 임의로 생성하지 않음
10. 불명확한 내용은 `[확인 필요]`로 남김

## 10. 검증 및 기록

작업 완료 후 가능한 검증을 모두 수행함.

```text
npm run lint
npm run typecheck
npm run test
npm run build
```

E2E 스크립트를 추가한 경우 해당 테스트도 실행함.

`DEVELOPMENT_LOG.md`에 다음 형식으로 기록함.

- 요구사항
- 기술적 문제
- 구현 방식
- 확인 결과
- 남은 한계

최종 보고에서는 다음을 구분함.

- 실제 구현
- 테스트로 확인한 항목
- 수동 확인 항목
- 미구현 또는 향후 확장
- `[확인 필요]`

## 11. 새 작업 세션에서 사용할 요청

```text
바다송금 v2.0 1차 구현을 진행해 주세요.
먼저 루트의 V2_IMPLEMENTATION_PLAN.md와 AGENTS.md를 끝까지 읽고 현재 코드와 문서를 분석해 주세요.

1차 단계에서는 docs/V2_PRODUCT_BRIEF.md와 구체적인 변경 계획을 작성하고, 아직 프로덕션 코드는 수정하지 마세요.
현재 v1.0에서 보존해야 할 동작, 기존 접근성 설정과 AssistanceProfile의 중복 지점, E2E 테스트 도입 방식, 변경 예정 파일을 보고해 주세요.
새로운 사실은 만들지 말고 불명확한 내용은 [확인 필요]로 표시해 주세요.
```

위 결과를 검토한 후 다음 요청으로 구현을 시작함.

```text
작성한 docs/V2_PRODUCT_BRIEF.md와 승인된 변경 계획을 기준으로 v2.0 1차 구현을 진행해 주세요.
AssistanceProfile, v1.0 E2E 기준선, 안내 방법 선택 화면까지 구현하고 lint, typecheck, test, build를 수행해 주세요.
실제 구현과 향후 계획을 구분해 DEVELOPMENT_LOG.md에 기록해 주세요.
```