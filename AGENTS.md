# Project Coding Guidelines

이 저장소에서 코드를 작성하거나 수정할 때 다음 원칙을 기본 규칙으로 적용한다.

"이 프로젝트는 다른 사람들도 항상 소스를 보게 되는 협업 프로젝트입니다.
20년차 프론트엔드 시니어 개발자 입장에서, 클린 코드 원칙(가독성, 일관된 네이밍, 적절한 함수 분리, 불필요한 주석 지양, 유지보수 용이성)을 지켜서 코드를 작성해주세요.
프로덕션 코드 리뷰를 통과할 수준으로 부탁드립니다."

1. 네이밍
   - boolean 이름은 `is`, `has`, `should` 접두사로 시작한다.
   - 이벤트 핸들러 이름은 `handle` 접두사로 시작한다.
   - 의미가 불분명한 약어를 피하고 역할이 드러나는 이름을 사용한다.
2. 단일 책임 함수
   - 함수는 한 가지 일만 담당한다.
   - 데이터 가공, API 호출, 상태 업데이트를 각각 분리한다.
3. 컴포넌트 분리
   - 상태 및 비즈니스 로직은 커스텀 훅으로 분리한다.
   - 표현과 사용자 인터페이스는 별도 컴포넌트로 구성한다.
4. 코드로 의도 표현
   - 설명용 주석에 의존하지 않는다.
   - 복잡한 조건식은 의미 있는 이름의 변수나 함수로 추출한다.
5. 매직 값 금지
   - 반복되거나 의미가 있는 숫자와 문자열은 이름 있는 상수로 선언한다.
6. Guard Clause
   - 조기 반환을 사용해 중첩 조건문을 단순화한다.
7. 상태 공유
   - props drilling이 3단계 이상 이어지면 Context 또는 적절한 상태 관리 방식을 검토한다.
8. 일관성
   - 저장소의 기존 네이밍, 문법, 포맷 및 구조적 스타일을 일관되게 유지한다.

모든 변경은 협업 환경에서 읽고 유지보수할 수 있는 프로덕션 수준으로 작성한다. 단, 무조건적인 추상화나 파일 분리는 피하고 변경 규모와 재사용성에 맞게 적용한다.

## `/refactoring` 요청

사용자가 `/refactoring`이라고 요청하면 대상 코드를 위 8가지 원칙으로 다시 검토한다. 발견한 문제를 수정하고, 관련 lint, typecheck, 테스트 또는 빌드 중 프로젝트에서 가능한 검증을 수행한다. 동작 변경이 필요한 경우에는 리팩터링과 구분해 명확히 알린다.
