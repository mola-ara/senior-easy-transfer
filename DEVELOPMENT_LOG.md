# DEVELOPMENT LOG

## 2026-09-03 — 프로젝트 분석 및 기획

- 무엇을 만들었는지: 안심송금 제품 기획서와 구현 범위를 작성했다.
- 어떤 기술을 사용했는지: 기존 Gradle/Java 구조와 Node 실행 환경을 확인했다.
- 어떤 문제였는지: 현재 폴더가 완전히 비어 있지 않고 Spring/Gradle 골격을 포함하며, Git 저장소 소유권 경고가 발생했다.
- 원인은 무엇이었는지: 향후 백엔드로 활용 가능한 기존 파일이 있었고, 상위 Git 저장소 소유자와 현재 실행 사용자가 달랐다.
- 어떻게 해결했는지: 기존 파일을 삭제하지 않고 프론트엔드는 `frontend/`, 문서는 `docs/`에 분리하기로 했다. Git 설정은 변경하지 않고 작업을 계속한다.
- 확인 결과: 기존 파일 목록과 Java 빌드 설정을 확인했고, 제품 문제·사용자·가설·MVP·흐름·화면·데이터·API 계획을 문서화했다.

## 2026-09-03 — 프론트엔드 MVP 구현

- 무엇을 만들었는지: 홈부터 송금 완료·오송금 안내까지의 전체 흐름, 최근 내역, 자주 보내는 분, 접근성 설정을 구현했다.
- 어떤 기술을 사용했는지: Next.js 16.3.4 App Router, React, TypeScript strict mode, Tailwind CSS 4, Lucide React, Context 상태 관리, Web Speech API를 사용했다.
- 어떤 문제였는지: 샌드박스에서 npm이 사용자 홈 경로를 읽지 못했고, 최초 lint가 프로젝트 내부 npm 캐시까지 탐색해 오래 걸렸다.
- 원인은 무엇이었는지: 실행 사용자에게 `C:\\Users\\taes1` 접근 권한이 없었고 ESLint 제외 목록에 `.npm-cache`가 없었다.
- 어떻게 해결했는지: npm 캐시를 `frontend/.npm-cache`로 지정하고 설치 명령에 필요한 실행 권한을 사용했다. ESLint 전역 제외 목록에 캐시 폴더를 추가했다.
- 확인 결과: 365개 패키지를 설치했고 npm 감사 결과 알려진 취약점은 0건이었다. 필수 화면과 조건부 안전 확인, 입력 검증, mock 서비스 계층을 연결했다.

## 2026-09-03 — 정적 검사 1차 수정

- 무엇을 만들었는지: ESLint 결과에 맞춰 설정 모듈과 접근성 저장값 복원 코드를 정리했다.
- 어떤 기술을 사용했는지: ESLint Next.js Core Web Vitals 및 React Hooks 규칙을 적용했다.
- 어떤 문제였는지: 익명 기본 내보내기 경고와 브라우저 저장값을 effect에서 복원하는 상태 변경 규칙 오류가 있었다.
- 원인은 무엇이었는지: PostCSS 설정 객체에 이름이 없었고, hydration 안전성을 위해 마운트 후 실행한 상태 복원을 최신 Hooks 규칙이 일반적인 effect 내 상태 변경으로 감지했다.
- 어떻게 해결했는지: 설정 객체에 이름을 부여했다. 로컬 저장소 복원은 브라우저 전용 동기화라는 이유를 코드에 기록하고 해당 한 줄에만 규칙 예외를 한정했다.
- 확인 결과: 변경 후 lint, 타입 검사, 빌드를 다시 실행한다.

## 2026-09-03 — 최종 검사 및 로컬 서버 확인

- 무엇을 만들었는지: 실행 안내 문서를 추가하고 전체 정적 검사, 프로덕션 빌드, 필수 경로 서버 점검을 완료했다.
- 어떤 기술을 사용했는지: ESLint, TypeScript compiler, Next.js Turbopack production build, PowerShell HTTP 요청을 사용했다.
- 어떤 문제였는지: 첫 서버 실행에서 일반적인 `C:\\Program Files\\nodejs` 위치를 사용해 npm 실행 파일을 찾지 못했다.
- 원인은 무엇이었는지: 이 PC는 Node.js를 NVM for Windows의 `C:\\nvm4w\\nodejs` 경로로 관리하고 있었다.
- 어떻게 해결했는지: `Get-Command`로 npm의 실제 경로를 확인한 뒤 숨김 프로세스로 프로덕션 서버를 다시 실행했다.
- 확인 결과: lint 오류 0건, TypeScript 오류 0건, Next.js 16.3.4 프로덕션 빌드 성공. 홈과 11개 필수 경로가 모두 HTTP 200을 반환했다. 서버는 `http://127.0.0.1:3000`에서 실행 중이다.

## 2026-09-03 — 고령 사용자를 위한 홈 화면 안내 개선

- 무엇을 만들었는지: 첫 방문 환영, 가상 잔액, 송금 시작, 바로 가기를 한 항목씩 설명하는 단계별 홈 안내와 다시 실행할 수 있는 `앱 알아보기` 버튼을 추가했다.
- 어떤 기술을 사용했는지: React 상태와 ref, localStorage 완료 기록, Web Speech API, dialog ARIA 속성, 키보드 포커스 트랩을 사용했다.
- 어떤 문제였는지: 한 화면에 여러 기능이 동시에 보이고 버튼 위치가 달라 고령 사용자가 각 기능의 의미와 다음 행동을 이해하기 어려울 수 있었다. 모바일 화면 오른쪽 스크롤바도 앱과 같은 인상을 방해했다.
- 원인은 무엇이었는지: 기존 홈은 모든 선택지를 한꺼번에 제공했으며 맥락별 안내와 시각적 초점이 없었다.
- 어떻게 해결했는지: 한 번에 한 영역만 밝게 강조하고 나머지는 흐리게 차단했다. 사용자가 `이해했어요`를 눌러야 다음 단계로 이동하며 이전 설명, 그만보기, 다시 듣기, 음성 끄기를 고정된 안내창에 제공했다. 세로 스크롤 기능은 유지하면서 브라우저별 스크롤바만 숨겼다.
- 확인 결과: lint 오류 0건, TypeScript 오류 0건, Next.js 프로덕션 빌드 성공. 새 빌드로 서버를 교체했으며 홈, 연습 안내, 받는 분 선택, 완료, 접근성 설정 경로가 모두 HTTP 200을 반환했다.

## 2026-09-03 — 홈 안내 강조 영역 구분 개선

- 무엇을 만들었는지: 송금하기와 바로 가기 안내 단계의 강조 경계를 더 명확하게 만들었다.
- 어떤 기술을 사용했는지: 안내 상태에 따른 조건부 클래스와 CSS 강조 카드, 설명 배지를 사용했다.
- 어떤 문제였는지: 안내 3단계 이후에는 여러 버튼이 한 영역에 있어 현재 설명 중인 범위를 한눈에 구분하기 어려웠다.
- 원인은 무엇이었는지: 기존 강조가 외곽선 위주였고 일반 홈 배경과 강조 영역의 면 구분이 약했다.
- 어떻게 해결했는지: 강조 대상을 흰색 독립 카드로 감싸고 노란 외곽선과 강한 그림자, `지금 설명하는 곳` 배지를 추가했다. 송금 단계에서는 권장 행동인 연습 송금을 녹색 테두리와 안내 문장으로 재강조하고 가상 송금은 잠시 약하게 표시했다.
- 확인 결과: 정적 검사와 프로덕션 빌드를 다시 실행한다.

## 2026-09-03 — 안내 대상과 설명창 겹침 수정

- 무엇을 만들었는지: 단계별 안내 대상과 하단 설명창이 서로 닿거나 겹치지 않는 화면 배치를 적용했다.
- 어떤 기술을 사용했는지: `scrollIntoView`의 시작점 정렬, `scroll-margin-top`, 안내 중에만 적용되는 하단 스크롤 여유를 사용했다.
- 어떤 문제였는지: 송금하기 카드의 아래 경계와 고정 설명창이 겹쳐 두 요소가 하나처럼 보이고 일부 내용이 가려졌다. 뒤쪽 바로 가기 단계에서도 같은 현상이 발생할 수 있었다.
- 원인은 무엇이었는지: 대상을 화면 가운데로 이동시키는 동안 하단 고정 설명창의 실제 높이를 위한 공간을 확보하지 않았다.
- 어떻게 해결했는지: 안내 대상을 화면 상단의 일정한 위치로 이동하고, 홈 콘텐츠 아래에 설명창 높이만큼 임시 스크롤 공간을 추가했다. 대상과 설명창에도 별도의 간격과 테두리를 부여했다.
- 확인 결과: 정적 검사와 새 프로덕션 빌드로 다시 확인한다.

## 2026-09-03 — 무스크롤 중심 안내 화면 개선

- 무엇을 만들었는지: 안내 단계에서 대상과 설명을 한 화면 안에 고정 배치하도록 변경했다.
- 어떤 기술을 사용했는지: 뷰포트 기준 고정 배치, 화면 높이 미디어 쿼리, `vh` 기반 최대 높이를 사용했다.
- 어떤 문제였는지: 겹침을 피하기 위해 추가한 스크롤 공간은 고령 사용자에게 다시 스크롤 조작을 요구했다.
- 원인은 무엇이었는지: 안내 대상을 일반 문서 흐름 안에서 이동시키는 구조였기 때문에 설명창 공간을 스크롤로 확보해야 했다.
- 어떻게 해결했는지: 안내 대상은 화면 위쪽, 설명창은 아래쪽에 고정했다. 작은 화면에서는 글씨와 터치 영역을 줄이지 않고 카드 여백과 요소 간격만 압축했다.
- 확인 결과: 안내 1단계부터 완료까지 별도의 사용자 스크롤 없이 진행할 수 있도록 구성했으며 정적 검사와 빌드를 다시 실행한다.

## 2026-09-03 — 금액 입력 이해도 개선

- 무엇을 만들었는지: 입력 금액의 한글 표기와 빠른 금액 버튼의 가산 동작 안내를 추가했다.
- 어떤 기술을 사용했는지: 원 단위 정수를 네 자리 단위로 읽는 TypeScript 한글 수 변환 함수와 `aria-live`를 사용했다.
- 어떤 문제였는지: 빠른 금액 버튼이 현재 금액을 바꾸는지 더하는지 알기 어렵고 큰 숫자를 숫자 표기만으로 재확인하기 어려웠다.
- 원인은 무엇이었는지: `+` 기호만으로 가산 동작을 전달했고 금액의 보조 읽기 표현이 없었다.
- 어떻게 해결했는지: 버튼마다 `더하기`를 표시하고 버튼 위에 반복해서 누르면 현재 금액에 더해진다고 설명했다. 입력값 아래에는 `한글로 육만 원`과 같은 표현을 실시간으로 제공한다.
- 확인 결과: 금액 변환과 화면 표시를 포함해 전체 검사를 다시 실행한다.

## 2026-09-03 — 금액 다시 입력과 실수 복구

- 무엇을 만들었는지: 입력 금액 전체 지우기와 방금 지운 금액 되돌리기를 추가했다.
- 어떤 기술을 사용했는지: 컴포넌트의 복구 가능 상태와 스크린 리더 상태 메시지를 사용했다.
- 어떤 문제였는지: 금액을 잘못 입력한 사용자가 숫자를 한 자리씩 지워야 했고, 전체 지우기 버튼도 잘못 누를 가능성이 있었다.
- 원인은 무엇이었는지: 다시 시작하는 명시적인 행동과 파괴적인 입력 변경을 복원할 방법이 없었다.
- 어떻게 해결했는지: `금액 모두 지우고 다시 입력`을 제공하고, 지운 직후 기존 금액과 `방금 지운 금액 되돌리기`를 함께 표시해 실수를 복구할 수 있게 했다.
- 확인 결과: 전체 정적 검사와 빌드에서 금액 재입력 흐름을 확인한다.

## 2026-09-03 — 가족·도우미 사용 준비와 쉬운 내비게이션

- 무엇을 만들었는지: 가족이나 은행 직원이 접근성을 미리 설정하고 사용자에게 바로 전달할 수 있는 사용 준비 화면을 만들었다. 하단 메뉴의 홈은 가운데로 옮기고 `처음 화면`으로 바꿨다.
- 어떤 기술을 사용했는지: 기존 접근성 Context, localStorage 안내 완료 기록 초기화, Web Speech API 시험 재생을 연결했다.
- 어떤 문제였는지: 고령 사용자는 `홈`이라는 디지털 용어와 집 아이콘의 의미를 모를 수 있고, 본인이 접근성 설정부터 해야 한다면 서비스 시작 전에 막힐 수 있었다.
- 원인은 무엇이었는지: 내비게이션이 서비스 관습을 이미 안다고 가정했고 설정 주체를 사용자 한 명으로만 보았다.
- 어떻게 해결했는지: 하단 중앙 버튼을 `처음 화면`으로 명확히 표현했다. 가족·도우미는 `/setup`에서 큰 글씨와 음성을 시험하고, 전달 버튼으로 첫 안내 기록을 초기화해 사용자에게 환영 단계부터 보여줄 수 있다.
- 확인 결과: 새 경로를 포함해 전체 검사를 실행한다.

## 2026-09-03 — 연습 시작 버튼 시각 안내

- 무엇을 만들었는지: 연습 시작 버튼 위에 아래 방향 화살표 세 개와 노란 갈색 테두리 강조 효과를 추가했다.
- 어떤 기술을 사용했는지: Lucide 아이콘, 순차 CSS 애니메이션, `prefers-reduced-motion` 대응을 적용했다.
- 어떤 문제였는지: 설명을 모두 읽은 뒤 실제로 눌러야 하는 연습 시작 버튼을 바로 찾기 어려울 수 있었다.
- 원인은 무엇이었는지: 시작 버튼이 다른 큰 버튼과 형태가 비슷해 시선 이동을 충분히 유도하지 못했다.
- 어떻게 해결했는지: 버튼을 향하는 화살표 세 개가 차례대로 움직이고 버튼 둘레가 노란 갈색으로 빛나도록 했다. 움직임 감소 설정에서는 고정 표시한다.
- 확인 결과: 연습 안내와 처음 화면 설명 완료 화면에 동일한 강조를 적용하고 검사를 실행한다.

## 2026-09-03 — 연습 안내 한 화면 배치

- 무엇을 만들었는지: 연습 안내의 중복 내용을 제거하고 시작·나중에 하기 버튼까지 한 화면에 들어오도록 재구성했다.
- 어떤 기술을 사용했는지: 화면 높이 반응형 CSS와 짧은 의미 단위 문장을 적용했다.
- 어떤 문제였는지: 설명 카드 세 개와 반복 안내 때문에 `다음에 할게요` 버튼이 화면 아래에서 잘렸다.
- 원인은 무엇이었는지: 같은 의미의 설명을 제목, 카드, 알림에서 반복해 세로 공간을 과도하게 사용했다.
- 어떻게 해결했는지: 안내를 `모두 가상 정보`, `틀려도 다시 수정` 두 항목으로 합치고 음성 재생 버튼과 중복 알림을 제거했다. 작은 화면에서는 여백만 추가로 줄였다.
- 확인 결과: 연습 시작 화살표와 두 행동 버튼을 포함해 한 화면 배치 기준으로 다시 검사한다.

## 2026-09-03 — 금액 다시 입력 단순화

- 무엇을 만들었는지: 금액을 지운 뒤 되돌리는 기능을 제거하고 `처음부터 다시 입력` 한 가지 행동만 남겼다.
- 어떤 기술을 사용했는지: 금액 입력 상태를 즉시 초기화하는 단순한 상태 변경을 적용했다.
- 어떤 문제였는지: `방금 지운 금액 되돌리기`는 선택지를 늘려 고령 사용자가 기능의 의미를 다시 판단해야 했다.
- 원인은 무엇이었는지: 실수 복구를 강조하면서 가장 단순해야 할 재입력 흐름에 별도 상태와 행동을 추가했다.
- 어떻게 해결했는지: 되돌리기와 상태 안내를 모두 제거했다. 사용자는 `처음부터 다시 입력`을 누른 뒤 빈 입력칸에 금액만 다시 넣으면 된다.
- 확인 결과: 금액 입력 화면을 다시 검사하고 빌드한다.

## 2026-09-03 — 수취인·금액·최종 확인 흐름 강화

- 무엇을 만들었는지: 받는 분 선택 확인창, 금액 변화 애니메이션, 10초 후 행동 안내, 최종 수취인 체크와 보내기 재확인창을 추가했다.
- 어떤 기술을 사용했는지: React 지연 타이머, CSS 크기 변화 애니메이션, 접근 가능한 모달 대화상자와 체크박스를 사용했다.
- 어떤 문제였는지: 받는 분을 잘못 눌러도 바로 다음 화면으로 넘어갔고 금액 버튼의 작은 설명은 읽기 어려웠다. 최종 보내기도 사용자의 명시적인 재확인이 부족했다.
- 원인은 무엇이었는지: 빠른 진행을 우선해 각 선택 직후의 멈춤과 확인 단계를 생략했다.
- 어떻게 해결했는지: 받는 분 선택 후 `맞나요?`를 예·아니요로 확인한다. 금액 버튼은 설명 대신 입력 숫자가 커지는 효과를 준다. 입력이 10초간 유지되면 확인 버튼을 강조한다. 최종 화면에서는 받는 분을 직접 체크하고 보내기 직전 다시 예·아니요를 선택한다.
- 확인 결과: 사용자 화면의 `가상 정보` 표현과 작은 금액 보조 문구를 제거하고 전체 검사를 실행한다.

## 2026-09-03 — 계좌 직접 입력 단계별 전환

- 무엇을 만들었는지: 계좌 직접 입력을 은행, 계좌번호, 이름, 최종 확인 순서로 한 항목씩 보여주는 흐름으로 재구성했다.
- 어떤 기술을 사용했는지: React 단계 상태, Visual Viewport API, 모바일 입력 속성, 부드러운 등장 애니메이션을 사용했다.
- 어떤 문제였는지: 여러 입력란이 한꺼번에 나타났고 모바일 키보드가 다음 버튼을 가릴 수 있었다.
- 원인은 무엇이었는지: 모든 필드를 한 화면에 배치했으며 휴대폰 키보드 높이를 고려하지 않았다.
- 어떻게 해결했는지: 현재 필요한 한 항목만 보여준다. 숫자판과 한글 키보드가 열리면 다음 버튼을 키보드 위에 고정한다.
- 확인 결과: 네 단계 입력과 키보드 대응을 포함해 전체 검사를 실행한다.

## 2026-09-03 — 큰 글씨 잘림과 의미 단위 줄바꿈 개선

- 무엇을 만들었는지: 큰 글씨 안내의 잘림을 제거하고 안내 문장을 짧은 의미 단위로 다시 작성했다.
- 어떤 기술을 사용했는지: CSS `word-break: keep-all`, 균형·가독성 줄바꿈, 문장별 블록 렌더링을 적용했다.
- 어떤 문제였는지: 큰 글씨 설정에서 고정 최대 높이 밖의 내용이 잘렸고 긴 문장이 화면 너비에 따라 의미와 무관한 위치에서 줄바꿈됐다.
- 원인은 무엇이었는지: 안내 대상에 `overflow: hidden`이 적용됐고 설명을 하나의 긴 텍스트 덩어리로 렌더링했다.
- 어떻게 해결했는지: 콘텐츠 숨김을 제거하고 큰 글씨에서는 내부 여백만 줄였다. 각 안내를 짧은 문장으로 바꾸고 한 문장씩 별도 줄에 표시해 의미 단위가 유지되도록 했다.
- 확인 결과: 큰 글씨와 작은 화면 조합을 기준으로 전체 검사를 다시 실행한다.

## 2026-09-03 — 피비콘과 웹앱 메타데이터

- 무엇을 만들었는지: 안심송금 전용 방패·확인 표시 SVG 피비콘과 웹앱 매니페스트를 추가했다.
- 어떤 기술을 사용했는지: Next.js App Router 메타데이터 파일 규칙, SVG, Web App Manifest를 사용했다.
- 어떤 문제였는지: 브라우저 탭과 모바일 홈 화면에서 프로젝트를 구분할 브랜드 아이콘이 없었다.
- 원인은 무엇이었는지: 기본 프로젝트에 아이콘 및 설치형 웹앱 메타데이터가 정의되지 않았다.
- 어떻게 해결했는지: 작은 크기에서도 선명한 코드 기반 벡터 아이콘을 만들고 이름, 시작 경로, 배경색, 테마색과 함께 연결했다.
- 확인 결과: 아이콘·매니페스트 경로를 포함해 프로덕션 빌드와 서버 응답을 확인한다.

## 2026-09-03 — 완료 화면 무스크롤·오송금 대응 개선

- 무엇을 만들었는지: 송금 완료 화면을 한 화면 중심으로 재배치하고 오송금 대응 버튼을 눈에 띄게 만들었다. 완료 음성은 자동 재생으로 변경했다.
- 어떤 기술을 사용했는지: 화면 높이 반응형 CSS, Web Speech API 생명주기 정리, 한 줄 계좌 표시를 적용했다.
- 어떤 문제였는지: 최근 내역 버튼이 화면 아래에서 잘리고 `잘못 보냈어요`가 작은 링크라 발견하기 어려웠다. 음성도 사용자가 별도 버튼을 눌러야 했다.
- 원인은 무엇이었는지: 완료 화면의 세로 여백과 영수증 행이 많았으며 중요한 오송금 대응보다 일반 이동 버튼이 먼저 표시됐다.
- 어떻게 해결했는지: 영수증을 세 행으로 줄이고 은행·계좌번호를 한 줄로 표시했다. 오송금 버튼을 영수증 바로 아래의 큰 주황색 버튼으로 올리고 처음 화면·최근 내역은 한 줄에 배치했다. 진입 즉시 결과를 읽고 `음성 안내 끄기`로 중단할 수 있게 했다.
- 확인 결과: 작은 화면과 큰 글씨를 고려한 완료 화면 CSS를 추가하고 전체 검사를 실행한다.

## 2026-09-03 — 고령 사용자 언어 기준 통일

- 무엇을 만들었는지: 사용자에게 보이는 `홈` 표현을 `처음 화면`으로 통일하고 새 기능 설명 원칙을 제품 문서에 추가했다.
- 어떤 기술을 사용했는지: 화면 문구 전수 검색과 제품 UX 작성 규칙을 적용했다.
- 어떤 문제였는지: `홈`은 디지털 서비스에 익숙하지 않은 사용자에게 이동 결과를 설명하지 못하는 용어였다.
- 원인은 무엇이었는지: 일반적인 모바일 앱 관습을 사용자가 이미 이해한다고 가정했다.
- 어떻게 해결했는지: 버튼을 누르면 어디로 가는지 직접 설명하는 `처음 화면으로 이동`으로 바꿨다. 향후 기능은 바로 가기에서 찾고, 첫 실행 전에 짧은 단계별 설명을 마친 뒤 시작하도록 규칙을 정했다.
- 확인 결과: 사용자 노출 문구에 남은 `홈` 표현이 없는지 다시 검색하고 전체 검사를 실행한다.

## 2026-09-03 — 바다송금 리브랜딩과 클린코드 리팩토링

- 무엇을 만들었는지: 앱 이름을 안심송금에서 바다송금으로 바꾸고, 브랜드 색을 그린 계열에서 바다블루 계열로, 파비콘을 방패 모양에서 파도 모티프로 교체했다. 밀집된 한 줄 JSX를 Prettier로 정리하고, 루트에 있던 미사용 Spring Boot/Gradle 스켈레톤을 삭제했다.
- 어떤 기술을 사용했는지: CSS 커스텀 프로퍼티 일괄 치환, Prettier(신규 도입), ESLint, TypeScript strict, Next.js Turbopack 프로덕션 빌드를 사용했다.
- 어떤 문제였는지: 루트 Spring 스켈레톤을 지우려고 `rm -rf ... src`를 실행했는데, 직전 명령의 `cd frontend`가 작업 디렉터리에 남아있는 상태에서 실행되어 지우려던 루트의 Java 스켈레톤이 아니라 `frontend/src` 전체(24개 파일)가 삭제됐다. 이 저장소는 아직 git 커밋이 하나도 없어 되돌릴 백업이 없었고, 사용자가 IDE에서 복구를 시도했지만 24개 중 22개는 빈 파일로만 복구되고 2개만 최신 내용으로 살아남았다.
- 원인은 무엇이었는지: 셸 작업 디렉터리가 명령 사이에 그대로 유지된다는 점을 확인하지 않은 채 상대 경로로 삭제 명령을 실행했다.
- 어떻게 해결했는지: 이번 대화에서 이미 읽었던 파일은 정확한 원본 내용으로 다시 작성했다. 읽지 않았던 파일(도메인 타입, 서비스 계층, 유효성 검증, 홈 투어, 금액·받는 분·안전 확인·최종 확인·오송금 안내·자주 보내는 분·최근 내역·연습 페이지)은 `docs/PRODUCT_BRIEF.md` 명세와 살아남은 `globals.css`의 클래스 구조를 근거로 다시 작성했다. 이후 삭제가 필요한 작업은 절대 경로로 대상을 재확인한 뒤에만 실행하기로 했다.
- 확인 결과: `tsc --noEmit` 오류 0건, `eslint` 오류 0건. `next build`에서 정적 생성 중 `/transfer/review`, `/transfer/safety`가 렌더링 중 `router.replace()`를 직접 호출해 `location is not defined` 오류를 냈고, 이를 `useEffect` 안으로 옮겨 해결한 뒤 17개 경로 모두 정적 생성에 성공했다. 프로덕션 서버 기동 후 처음 화면을 포함한 13개 핵심 경로 모두 HTTP 200을 확인했다. 다만 다시 작성된 페이지의 세부 로직(금액 위험 신호 기준, 홈 투어 진행 방식 등)은 원본과 완전히 동일하다는 보장이 없어 실제 화면에서 추가 확인이 필요하다.

## 2026-09-03 — GitHub·Vercel 연결

- 무엇을 만들었는지: 잘못된 위치(상위 폴더)에 있던 git 저장소를 실제 프로젝트 폴더로 옮기고, GitHub 공개 저장소를 생성해 push했다. Vercel 배포까지 연결하고 문서에 배포 링크를 반영했다.
- 어떤 기술을 사용했는지: GitHub CLI(`gh repo create --source=. --push`), Vercel(웹 UI, Root Directory를 `frontend`로 지정)을 사용했다.
- 어떤 문제였는지: 첫 커밋이 `senior-easy-trasnfer`가 아니라 그 상위 폴더를 저장소 루트로 잡고 있어서, 그대로 push하면 GitHub에서 README가 저장소 홈에 바로 보이지 않는 구조가 될 뻔했다.
- 원인은 무엇이었는지: 이전 세션에서 상위 폴더에 이미 git이 초기화되어 있었고(Git 저장소 소유권 경고로 인해 그대로 두기로 했던 이력), 이번 세션에서도 그 위치 그대로 커밋을 진행했다.
- 어떻게 해결했는지: 아직 원격 연결 전(로컬 커밋 1개뿐)이라는 점을 확인한 뒤, 프로젝트 폴더 안에 새로 `git init`하고 동일한 내용으로 다시 커밋했다. 상위 폴더의 기존 `.git`은 삭제했다. 이후 `gh repo create`로 `senior-easy-transfer` 공개 저장소를 만들고 push했다.
- 확인 결과: `https://github.com/mola-ara/senior-easy-transfer`에 정상 push됨을 확인했다. Vercel에 Root Directory를 `frontend`로 지정해 배포했고, `https://senior-easy-transfer-ya2w.vercel.app/`가 HTTP 200으로 응답함을 확인했다.
